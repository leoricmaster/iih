# CLAUDE.md

智能情报中心（IIH）— 多智能体情报生产与研判系统。当前阶段：概念建模（尚无代码）。

## 术语纪律（最重要）

- `docs/glossary.md` 是全项目术语的单一事实来源；一切输出（文档、代码标识符、提交信息）必须使用表内术语，English 列即代码命名标准。
- 修改/新增术语：先入候选区（🔶），用户裁决后转正（✅），同步更新 `docs/domain-model.md`。
- 已确立术语禁止同义词混用。

## 架构原则

- **判断归智能体，记账归确定性系统**：判断层（定向 / 采集 / 审查 / 核实 / 分析智能体）｜记账层（状态机、信用计算、溯源存储、分发检索、调度）｜工具层（无状态传统功能，作为智能体工具）。
- 智能体对记账层的写入一律为**提案**（产出 + 依据），经校验落账；**无溯源不落账**。
- 评级与置信度：智能体测变量（结构化证据评估），公式出结论（版本化、可重放，公式版本记入推理记录）。

## 工作方式

- 单人开发 + AI 辅助，中长期。设计决策经讨论后由用户裁决，落进 `docs/adr/`。涉及已决事项时先读对应 ADR，不重复讨论。
- 文档风格：简洁精练，能删则删。
- 语言：文档用中文；代码标识符用英文（对应术语表 English 列）。

## 需求与任务管理

- 用 Backlog.md 管理需求与任务（`backlog/` 目录，任务 ID 前缀 `IIH-`）。
- 每次会话开始先运行 `backlog instructions overview` 获取工作流；创建任务前先 `backlog search` 查重。
- 任务描述、验收标准一律用术语表内术语；涉及架构决策的任务须在描述中关联对应 ADR。
- 不手编 `backlog/` 下的 markdown，一律走 `backlog` CLI，保证元数据一致。
